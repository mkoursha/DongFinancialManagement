import 'package:flutter/material.dart';

class AboutUs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("About US"),
      ),
      body: Center(
        child: Text("تا دنگو پس نگیریم آروم نمیگیگیریم"),
      ),
    );
  }
}
